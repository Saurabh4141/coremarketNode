ALTER TABLE report_master
ADD COLUMN SubIndustryId int(11) DEFAULT NULL AFTER IndustryId,
ADD KEY idx_subindustry (SubIndustryId, IsActive),
ADD CONSTRAINT fk_report_subindustry
  FOREIGN KEY (SubIndustryId)
  REFERENCES sub_industries_master (Id)
  ON DELETE SET NULL;

ALTER TABLE report_master
ADD COLUMN forecast_year_from VARCHAR(10) DEFAULT NULL  AFTER Description,
ADD COLUMN forecast_year_to VARCHAR(10) DEFAULT NULL  AFTER forecast_year_from;
ADD COLUMN market_size VARCHAR(25) DEFAULT NULL  AFTER forecast_year_to;
ADD COLUMN growth_rate VARCHAR(25) DEFAULT NULL  AFTER market_size;
ADD COLUMN regions VARCHAR(200) DEFAULT NULL  AFTER growth_rate,
ADD COLUMN report_version VARCHAR(20) DEFAULT '2024'  AFTER currency;


ALTER TABLE report_master
ADD COLUMN currency CHAR(3) DEFAULT 'USD' AFTER Path,
ADD COLUMN seo_title VARCHAR(255) DEFAULT NULL AFTER Corp_User_Prize,
ADD COLUMN seo_description VARCHAR(300) DEFAULT NULL AFTER seo_title,
ADD COLUMN sample_pdf_path VARCHAR(255) DEFAULT NULL AFTER seo_description,
ADD COLUMN customization_available TINYINT(1) DEFAULT 0 AFTER sample_pdf_path;


ALTER TABLE report_master
ADD COLUMN report_status ENUM('draft','published','archived') DEFAULT 'published'  AFTER noindex,
ADD COLUMN publish_at DATETIME DEFAULT NULL  AFTER report_status;

ALTER TABLE report_master
ADD COLUMN canonical_url VARCHAR(255) DEFAULT NULL  AFTER report_status,
ADD COLUMN noindex TINYINT(1) DEFAULT 0  AFTER canonical_url;


ALTER TABLE report_master
ADD COLUMN report_type ENUM('syndicated','custom','subscription') DEFAULT 'syndicated'  AFTER report_status;








